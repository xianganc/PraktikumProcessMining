FROM ubuntu:18.04
ENV INITSYSTEM on


RUN mkdir \
  /deps \
  /src \
  /run/sshd \
  /root/.ssh \
  /var/www/html

COPY ./deps/ /deps/
COPY ./webapp/ /var/www/

RUN chmod +x /deps/*.sh && /deps/deps.sh

CMD /deps/init.sh