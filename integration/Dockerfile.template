FROM ubuntu:18.04
ENV INITSYSTEM on


RUN mkdir \
  /deps \
  /src

COPY ./deps/ /deps/
COPY ./src/ /src/
COPY ./bin/* /usr/sbin/

RUN chmod +x /deps/deps.sh && /deps/deps.sh
RUN chmod +x /usr/sbin/* && chmod +x /deps/init.sh

CMD /deps/init.sh
